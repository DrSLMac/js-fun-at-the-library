function createLibrary(denverLibrary) {
var libraryInfo = {
  name: "Denver Public Library",
}
return libraryInfo.name
}
// next step is to name the library
//parameter would be libraryName
//function createLibrary(libraryName) {
// dot notation indicates an object.


module.exports = {
  createLibrary,
  // addBook,
  // checkoutBook
};
